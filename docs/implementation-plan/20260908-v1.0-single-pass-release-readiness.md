# Single-pass release readiness — 2026-09-08

Status: candidate review in progress; merge and publication blocked by acceptance.

The user authorized completing verification, merging, and publishing on
2026-09-08. Authorization does not turn failed or missing checks into passes.
The existing `codex/single-pass-autofit` worktree is the implementation source;
root `main` now matches published 0.2.0 at `85c3519`.

## Candidate and fresh local checks

The working candidate includes the canonical single-stream ABI v6 writer and
its subsequent display-aware width estimator. Production files under `crates/`
and `python/`, plus Cargo manifests/lock and `pdm.lock`, match the retained
`candidate-cjk-ab843776df868033` source snapshot byte for byte.

Fresh checks on 2026-09-08:

- `CARGO_BUILD_JOBS=2 pdm run check`: passed; Python 140 passed, two explicitly
  gated large tests skipped; Rust 27 passed; static checks and Linux release
  wheel validation passed.
- `NEATXLSX_INCLUDE_LARGE=1 pdm run pytest tests/integration/test_format_overrides.py -k pagination -q`:
  passed both real Excel-limit tests (14 unrelated tests deselected).
- Retained display-autofit evidence checksum manifest: passed.
- Local wheel SHA-256:
  `88e6dcb3849bad1760376963e111e890c9cc4f99cb6a3e9eaa68ceb6b65efcc6`.

The wheel remains version 0.2.0 as a local candidate artifact. It is not the
published 0.2.0 artifact and must never replace or be uploaded as that version.
Version promotion requires acceptance and its own final build.

## Performance evidence

Reanalyzing the unchanged 108 raw records with the current analyzer preserves
five failing verdicts and one passing verdict. `finalize_comparison()` returns
1 and `acceptance_passed` is false. This is reanalysis, not a new measurement.
The first four intervals do not exclude the specified 3% regression bound;
that alone does not prove their median runtime is materially worse. Full-scan
`body` has a median ratio of 1.0731 and upper bound 1.1539, exceeding its 1.15
upper-bound limit. Full-scan `all` passes its separate budget.

A fresh comparison uses the unchanged frozen six scenarios, four ABBA/BAAB
blocks, one warmup per variant, eight measured samples per variant, CPUs 0–7,
four Polars threads, and the existing `display-autofit` policy. The candidate
is an immutable source copy with a freshly built native extension. The source
manifest SHA-256 is
`c411b68a7a2c1c64df3333ec427a9715ab06f0ae49c45a2674f906f362cb6b5f`.
The baseline is the pre-estimator single-pass source at `1ca1040`, using its
retained release native SHA-256
`7f97fa1597fc1f59c5792833197a04d5e02c6f447340bbced27a9b2a94cda33d`.
Results will be recorded separately; the historical failure is not overwritten.

## Remaining release boundaries

- Fresh display-estimator performance verdict and the separate original
  single-pass adoption gate; neither measures the other upgrade by itself.
- Windows desktop Excel visual inspection and numeric-cell edit behavior
  required by the display-autofit plan. Linux OOXML checks and a Windows wheel
  import do not establish this behavior.
- Final-head CI and the five-platform wheel/sdist release bundle.
- A new version, immutable bundle publication, and PyPI/GitHub readback only
  after the relevant prerequisites pass.

The native-zlib experiment remains rejected. No performance threshold, output
oracle, sampling policy, or diagnostic is relaxed in this readiness pass.
